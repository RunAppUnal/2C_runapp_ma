mutation LoginMutation(
  $email: String!
  $password: String!
  ){
  login(
     email: $email,
     password: $password
  ){
     id
     name
     username
     lastname
     cellphone
     token
     client
     uid
     email
  }
}